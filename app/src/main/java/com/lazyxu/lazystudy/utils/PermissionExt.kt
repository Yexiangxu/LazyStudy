package com.lazyxu.lazystudy.utils

class PermissionExt {
    /**
     * 检查APP是否应该向用于展示申请权限的解释
     * 当APP从未申请过指定的权限或申请了指定权限，但被用户拒绝，且勾选了【不再提示】，返回false；
     * APP申请指定权限时被用户拒绝，但未勾选【不再提示】，返回true
     */
//    override fun shouldShowRequestPermissionRationale(permission: String): Boolean {
//        return super.shouldShowRequestPermissionRationale(permission)
//        LogUtils.tag("Tag265").d("shouldShowRequestPermissionRationale permissions=$permissions")
//    }
}